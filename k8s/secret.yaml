apiVersion: v1
kind: Secret
metadata:
  name: transaction-api-secrets
type: Opaque
data:
  # echo -n "sa" | base64
  datasource-username: c2E=
  # echo -n "password" | base64
  datasource-password: cGFzc3dvcmQ=